<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_select_move"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    tools:context="com.codeclan.example.rockpaperscissors.SelectMove">

    <Button
        android:id="@+id/rock_button"
        android:onClick="playerPicksRock"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/rock_button"
        android:textAlignment="center"
        android:textSize="50dp"
        />

    <Button
        android:id="@+id/paper_button"
        android:onClick="playerPicksPaper"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/paper_button"
        android:textAlignment="center"
        android:textSize="50dp"
        />

    <Button
        android:id="@+id/scissors_button"
        android:onClick="playerPicksScissors"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/scissors_button"
        android:textAlignment="center"
        android:textSize="50dp"
        />

</LinearLayout>
